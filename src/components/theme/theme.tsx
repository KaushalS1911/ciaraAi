"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    textGray: "#667085",
    blue: "#1D2939",
    darkBlue: "#344054",
  },
});

export default theme;
